import { LogoutButton } from "../logout-button";
import { navigation } from "../mock-data";
import { Icon } from "./icon";

export function Sidebar({ user }: { user: { name: string; email: string; image?: string | null } }) {
  return <aside className="dashboard-sidebar"><div className="flex items-center gap-3 px-3"><span className="grid size-8 shrink-0 place-items-center rounded-[10px] bg-accent text-sm font-bold text-accent-foreground">Z</span><span className="sidebar-label text-[15px] font-semibold tracking-[-0.02em]">Zury</span></div><nav className="mt-9 space-y-1" aria-label="Primary navigation">{navigation.map((item) => item.active ? <a key={item.label} href="/dashboard" aria-current="page" className="sidebar-item sidebar-item-active"><Icon name={item.icon} /><span className="sidebar-label">{item.label}</span><span className="ml-auto size-1.5 rounded-full bg-accent" /></a> : <span key={item.label} aria-disabled="true" className="sidebar-item cursor-default"><Icon name={item.icon} /><span className="sidebar-label">{item.label}</span></span>)}</nav><div className="mt-auto space-y-3"><div className="flex items-center gap-3 border-t border-border px-3 pt-4"><span className="grid size-8 shrink-0 place-items-center overflow-hidden rounded-full bg-surface-hover text-xs font-semibold text-text-secondary">{user.image ? <img src={user.image} alt="" className="size-full object-cover" /> : user.name.slice(0, 1).toUpperCase()}</span><span className="sidebar-label min-w-0"><span className="block truncate text-sm font-medium text-text-primary">{user.name}</span><span className="block max-w-36 truncate text-xs text-text-tertiary">{user.email}</span></span><button className="ml-auto hidden text-text-tertiary transition hover:text-text-primary lg:block" aria-label="Account menu"><Icon name="more" size={16} /></button></div><div className="sidebar-label"><LogoutButton /></div></div></aside>;
}
